// Series Utility Functions

// Sums the first n positive integers
pub fn sum_first_integers(n: i32) -> i32 {
    n * (n + 1) / 2
}

// Sums the first n positive squares
pub fn sum_first_squares(n: i32) -> i32 {
    n * (n + 1) * (2 * n + 1) / 6
}
